// The app's standard image aspect ratio, matching how grid, post, and ad images
// are actually displayed in the reader app — keeps every uploaded image cropped
// consistently instead of leaving it to whatever the source photo was.
export const IMAGE_ASPECT = [390, 422];
export const IMAGE_ASPECT_RATIO = 390 / 422;
